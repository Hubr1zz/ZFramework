---
name: project-hunting-in-darkness
description: Hunting in Darkness 迁入 ZFramework 后的领域内容速查。用于理解玩法目录、命名空间、三层规则和原项目限制；TEngine 框架事实仍以 project-context 与 tengine-dev 为准。
---

# Hunting in Darkness Project Context

本 skill 是从原项目迁入的项目内容层资料，不覆盖 ZFramework 的 TEngine 架构资料。
references 中的旧 `Assets/Scripts/` 路径统一映射到
`Assets/GameScripts/GameLogic/HuntingInDarkness/`；原文保留用于审计。

## 必读入口

- [PROJECT-INDEX.md](references/PROJECT-INDEX.md)

## 使用规则

先按索引跳到任务命中的参考和代码，不得默认读取其余 references。若文档与当前代码冲突，以代码为准，并用 `workflow-reflection` 记录差异。
