## Implementation

- [x] Move `SettlementEventWork` execution and restore continuation into `PlayableSettlementPhaseCoordinator`.
- [x] Remove unused `EventData`/occurrence overloads from `GameManager`.
- [x] Add narrow generation/reset/parallel-runner coverage and run affected EditMode/PlayMode tests.
- [x] Run strict OpenSpec validation, targeted compilation and diff checks.
- [ ] Human review approves syncing this Delta Change into the formal Specs.
