# Claude Code 薄入口

读取并遵守项目根 `AGENTS.md`。完整规则位于共享 `.agents/`；`.claude/` 只保存 Claude Code 入口、命令和 Agent wrapper，不维护正文副本。

生成或修改 TEngine C# 时，必须读取 `.agents/skills/tengine-dev/references/CODE-WORKFLOW.md`。
